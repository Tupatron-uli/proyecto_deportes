<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Menú Profesional</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <style>
    html, body {
      margin: 0;
      padding: 0;
      height: 100%;
      overflow: hidden;
      background-color: black;
      font-family: 'Segoe UI', sans-serif;
      position: relative;
    }

    /* Fondo de estrellas */
    body::before {
      content: '';
      position: fixed;
      top: 0; left: 0;
      width: 100vw;
      height: 100vh;
      background: 
        radial-gradient(2.5px 2.5px at 15% 25%, white, transparent),
        radial-gradient(2px 2px at 40% 55%, #ccc, transparent),
        radial-gradient(3px 3px at 65% 85%, #eee, transparent),
        radial-gradient(1.5px 1.5px at 85% 15%, #bbb, transparent),
        radial-gradient(2.2px 2.2px at 30% 70%, #ddd, transparent);
      background-repeat: repeat;
      background-size: 100% 100%;
      animation: estrellasMover 150s linear infinite;
      z-index: 1;
      pointer-events: none;
      filter: brightness(0.7);
    }

    @keyframes estrellasMover {
      0% { background-position: 0 0; }
      100% { background-position: 1200px 600px; }
    }

    /* Menú */
    nav {
      position: relative;
      z-index: 10;
      background-color: #24292e;
      padding: 0.5rem 1rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
      color: white;
    }

    .nav-left, .nav-right {
      display: flex;
      align-items: center;
      gap: 10px;
    }

    .dropdown {
      position: relative;
    }

    .dropdown-content {
      display: none;
      position: absolute;
      background-color: white;
      min-width: 200px;
      box-shadow: 0px 4px 8px rgba(0,0,0,0.1);
      z-index: 11;
      top: 100%;
      left: 0;
    }

    .dropdown-content a {
      color: #333;
      padding: 10px;
      text-decoration: none;
      display: block;
    }

    .dropdown-content a:hover {
      background-color: #f1f1f1;
    }

    .dropdown:hover .dropdown-content {
      display: block;
    }

    .nav-link {
      color: white;
      text-decoration: none;
      padding: 6px 12px;
      border: 1px solid transparent;
      border-radius: 4px;
    }

    .nav-link:hover {
      background-color: #444c56;
      border-color: #1b1f23;
    }

    .btn {
      background-color: #2ea44f;
      color: white;
      padding: 6px 12px;
      border: none;
      border-radius: 4px;
      text-decoration: none;
    }

    .btn:hover {
      background-color: #2c974b;
    }

    /* Carrusel de imágenes */
    .fondos {
      position: absolute;
      top: 0;
      left: 0;
      display: flex;
      width: calc(100vw * 8);
      height: 100%;
      animation: moverFondos 150s linear infinite;
      z-index: 2;
    }

    .fondo {
      flex: 0 0 100vw;
      height: 100%;
      background-size: cover;
      background-repeat: no-repeat;
      background-position: center center;
      background-attachment: fixed;
      object-fit: cover;
      filter: brightness(0.85) contrast(1.1);
      mix-blend-mode: screen;
      box-shadow: inset 50px 0 50px -30px black, inset -50px 0 50px -30px black;
    }

    .fondo1 { background-image: url('/images/fidalgo.jpg'); }
    .fondo2 { background-image: url('/images/curry.jpg'); }
    .fondo3 { background-image: url('/images/ronaldo.jpg'); }
    .fondo4 { background-image: url('/images/siu.jpg'); }
    .fondo5 { background-image: url('/images/americano1.jpg'); }
    .fondo6 { background-image: url('/images/americano2.jpg'); }
    .fondo7 { background-image: url('/images/volei1.jpg'); }
    .fondo8 { background-image: url('/images/volei2.jpg'); }

    @keyframes moverFondos {
      0% { transform: translateX(0); }
      100% { transform: translateX(-700vw); }
    }

    /* Texto superpuesto */
    .contenido {
      position: fixed;
      z-index: 9;
      color: white;
      text-align: center;
      top: 40vh;
      width: 100%;
      font-family: Arial, sans-serif;
      text-shadow: 0 0 10px black;
    }

    .contenido h1 {
      font-size: 3rem;
      background: rgba(0, 0, 0, 0.4);
      padding: 0.5rem 1rem;
      border-radius: 10px;
      display: inline-block;
    }

    .contenido p {
      font-size: 1.5rem;
      background: rgba(0, 0, 0, 0.4);
      padding: 0.5rem 1rem;
      border-radius: 10px;
      display: inline-block;
      margin-top: 1rem;
    }
  </style>
</head>
<body>

  <!-- Menú de navegación -->
  <nav>
    <div class="nav-left">
      <strong>Mi App</strong>
      <div class="dropdown">
        <a class="nav-link" href="#">Formularios ▼</a>
        <div class="dropdown-content">
          <a href="/form">Formulario de deporte de inscripción</a>
          <a href="/formtutor">Formulario de autorización para los padres</a>
          <a href="/formdescuento">Formulario de descuento para la inscripción</a>
          <a href="/reglas">Reglas</a>
          <a href="/dashboard">Panel</a>
          <a href="/cierre">Cerrar</a>
        </div>
      </div>
    </div>
    <div class="nav-right">
      <a href="/login" class="nav-link">Iniciar Sesión</a>
      <a href="/register" class="btn">Registrarse</a>
    </div>
  </nav>

  <!-- Carrusel de fondos -->
  <div class="fondos">
    <div class="fondo fondo1"></div>
    <div class="fondo fondo2"></div>
    <div class="fondo fondo3"></div>
    <div class="fondo fondo4"></div>
    <div class="fondo fondo5"></div>
    <div class="fondo fondo6"></div>
    <div class="fondo fondo7"></div>
    <div class="fondo fondo8"></div>
  </div>

  <!-- Texto sobre las imágenes -->
  <div class="contenido">
    <h1>Bienvenidos al Mundo del Deporte</h1>
    <p>Explora lo mejor del básquet, fútbol, tochito y vóley</p>
  </div>

</body>
</html>
