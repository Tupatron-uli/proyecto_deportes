<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Formulario <%= deporte %></title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <div class="container">
    <h2>Formulario de <%= deporte %></h2>
    <form action="<%= datos ? ('/actualizar/' + datos.id) : ('/guardar/' + deporte) %>" method="POST">
      
      <label for="nombre">Nombre:</label>
      <input type="text" id="nombre" name="nombre" value="<%= datos?.nombre || '' %>" required>

      <label for="apellido_paterno">Apellido Paterno:</label>
      <input type="text" id="apellido_paterno" name="apellido_paterno" value="<%= datos?.apellido_paterno || '' %>" required>

      <label for="apellido_materno">Apellido Materno:</label>
      <input type="text" id="apellido_materno" name="apellido_materno" value="<%= datos?.apellido_materno || '' %>" required>

      <label for="email">Email:</label>
      <input type="email" id="email" name="email" value="<%= datos?.email || '' %>" required>

      <label for="fecha_nacimiento">Fecha de nacimiento:</label>
      <input type="date" id="fecha_nacimiento" name="fecha_nacimiento" value="<%= datos?.fecha_nacimiento?.substring(0, 10) || '' %>" required>

      <label for="salud">Problemas de salud:</label>
      <textarea id="salud" name="salud"><%= datos?.salud || '' %></textarea>

      <label for="domicilio">Domicilio:</label>
      <input type="text" id="domicilio" name="domicilio" value="<%= datos?.domicilio || '' %>" required>

      <label for="telefono">Número telefónico:</label>
      <input type="tel" id="telefono" name="telefono" value="<%= datos?.telefono || '' %>" required>

      <label for="sexo">Sexo:</label>
      <select id="sexo" name="sexo" required>
        <option value="">Seleccione sexo</option>
        <option value="Masculino" <%= datos?.sexo === 'Masculino' ? 'selected' : '' %>>Masculino</option>
        <option value="Femenino" <%= datos?.sexo === 'Femenino' ? 'selected' : '' %>>Femenino</option>
        <option value="Otro" <%= datos?.sexo === 'Otro' ? 'selected' : '' %>>Otro</option>
      </select>

      <button type="submit"><%= datos ? 'Actualizar' : 'Guardar' %></button>
      <a href="/dashboard">Cancelar</a>
    </form>
  </div>
</body>
</html>
